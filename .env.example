# Copy this file to .env.local and fill the values

# reCAPTCHA v3 site key for Firebase App Check (required in production)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=

# App Check debug token for local dev. Set to 'true' to have Firebase print a token in console,
# then replace with that token string for subsequent runs.
# Example: NEXT_PUBLIC_APPCHECK_DEBUG_TOKEN=1234567890abcdef
NEXT_PUBLIC_APPCHECK_DEBUG_TOKEN=true
